name: Deploy Prod

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: prod
      api_service_name: mt-oil-api-prod
      tf_dir: infra/environments/prod
      frontend_bucket: my-project-1508887546225-mt-oil-dashboard
